<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit The Chat</title>
  </head>
  <body>
    <h2>You are updating the msg</h2>
    <h3>Conversation is in between <%= chat.from %> and <%= chat.to %></h3>

    <form method="post" action="/chats/<%= chat.id %>?_method=PATCH">
      <textarea name="msg" id="" cols="30" rows="10">
        <%= chat.msg %>
      </textarea>
      <button>Submit To Change</button>
    </form>
  </body>
</html>
